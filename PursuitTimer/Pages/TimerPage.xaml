<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:PursuitTimer.ViewModels"
             xmlns:res="clr-namespace:PursuitTimer.Resources.Strings"
             x:Class="PursuitTimer.Pages.TimerPage"
             x:DataType="pageModels:TimerViewModel"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="{x:Static res:AppResources.Timing}"
             BackgroundColor="{Binding Splitcolor}">


   <Grid 
        x:Name="TimerLayout">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding SplitCommand}" />
        </Grid.GestureRecognizers>

        <Label
            x:Name="LastSplitLabel"
            FontAttributes="Bold"
            FontSize="{Binding Fontsize}"
            VerticalTextAlignment="Center"
            HorizontalTextAlignment="Center"
            Text="{Binding Splittext}"
            TextColor="{Binding Splittextcolor}"/>
    </Grid>
</ContentPage>
